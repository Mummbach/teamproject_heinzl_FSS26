# PRD-Net pipeline — dependencies pinned to the versions the reported results
# were produced with (Python 3.13, macOS/CPU; MPS is not used, GRU trains on CPU).
#
#   python3 -m venv venv
#   source venv/bin/activate            # Windows: venv\Scripts\activate
#   pip install -r pipeline/requirements.txt
#
# Core data / modelling
numpy==1.26.4
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0
pyarrow==23.0.1
torch==2.10.0
tqdm==4.67.3

# Visualisation / explanation
matplotlib==3.10.8      # 05_analysis EDA plots (optional stage)
seaborn==0.13.2         # correlation.py, 12_ts_monitoring.py, 13_ts_monitoring_regimes.py
plotly==6.6.0           # fd07_report.py standalone HTML dashboards
shap==0.49.1            # fd05_diff-explain.py  (w·Δ == SHAP sanity check)
streamlit==1.58.0       # fd07_dashboard.py     (interactive dashboard)

# Optional — only needed by specific track scripts, NOT part of the validated
# core run (unpinned: not installed in the reference environment):
optuna                  # 08_hyperparameter_search.py (GRU hyperparameter search)
transformers            # 02b_cxr_features.py / 01e_extract_bioclinicalbert_embeddings.py (BioClinicalBERT)
umap-learn              # 14_patient_mii_clustering.py (falls back gracefully if absent)
